# Real config (only keep the example config in git)
configs/supabase-projects.json

# Supabase environment files (per project)
**/.env

# Let's Encrypt / ACME data
acme.json

# Logs
*.log

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
